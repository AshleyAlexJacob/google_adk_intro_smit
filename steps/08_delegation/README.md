# Step 8 — Agent-to-agent delegation

Sub-agents can `transfer_to_agent`. ADK keeps the conversation state.

```bash
python chat.py
```

Try (same session, no restart):

- `What class is on Monday?` then `What is that session about?`
\n