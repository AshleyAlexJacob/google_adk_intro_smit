# Step 4 — MCP and custom tools

Schedule tool plus `read_campus_doc`. MCP can also serve the `docs/` folder.

```bash
python chat.py
```

Try:

- `What class is on Monday?`
- `Summarize the campus guide.`
- `Who do I ask about API keys?`
\n